/// Settings config constants.
abstract final class SettingsConfig {
  /// Settings folder.
  static const folder = 'settings';

  /// App settings filename.
  static const filename = 'app.json';

  /// Minimum font size.
  static const double minFontSize = 9;

  /// Maximum font size.
  static const double maxFontSize = 96;

  /// App settings structure version.
  static const version = 1;
}
